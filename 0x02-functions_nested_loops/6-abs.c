#include "main.h"

/**
 * _abs - Computes the absolute value of an integer.
 * @i: Input number as an integer.
 *
 * Return: (i) if i is greater than or equal to 0, (-i) if i is less than 0.
 */
int _abs(int i)
{
	if (i >= 0)
	{
		return (i);
	}
	else
	{
		return (-i);
	}
}
